# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [AzurePipelines (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration AzurePipelines_backend --host AzurePipelines
#
# </auto-generated>
# ------------------------------------------------------------------------------

stages:
  - stage: ubuntu_latest
    displayName: 'ubuntu-latest'
    dependsOn: [  ]
    pool:
      vmImage: 'ubuntu-latest'
    jobs:
      - job: BackendAll
        displayName: 'BackendAll'
        dependsOn: [  ]
        steps:
          - checkout: self
            fetchDepth: 0
          - task: Cache@2
            displayName: 'Cache: nuke-temp'
            inputs:
              key: $(Agent.OS) | nuke-temp | **/global.json, **/*.csproj, **/Directory.Packages.props
              restoreKeys: $(Agent.OS) | nuke-temp
              path: .nuke/temp
          - task: Cache@2
            displayName: 'Cache: nuget-packages'
            inputs:
              key: $(Agent.OS) | nuget-packages | **/global.json, **/*.csproj, **/Directory.Packages.props
              restoreKeys: $(Agent.OS) | nuget-packages
              path: $(HOME)/.nuget/packages
          - task: CmdLine@2
            displayName: 'Run: BackendClean, BackendRestore, BackendBuild, BackendTests, BackendTestsCodeCoverage, BackendAll'
            inputs:
              script: './build.cmd BackendClean BackendRestore BackendBuild BackendTests BackendTestsCodeCoverage BackendAll --skip'
          - task: PublishBuildArtifacts@1
            displayName: 'Publish: TestResults'
            inputs:
              artifactName: TestResults
              pathToPublish: 'NukeWorkshops.Server.Tests/TestResults'
